# MockLocationApp

Android Mock Location with Python Control

## Usage

1. Install APK: `adb install app-debug.apk`
2. Set as mock location app in Developer Options
3. Run: `python python/control_location.py`
